# 2026-02-10 - EduFunds Deployment

## Erledigt

### Docker Image Build & Deploy
- Neues Image `edufunds:fresh` gebaut
- Container läuft healthy im hetzner-stack_web Netzwerk
- Interne Tests zeigen korrekte Daten:
  - **43 Förderprogramme** (nicht mehr 22)
  - Keine falschen Statistiken (500+ Schulen entfernt)
  - Neue Seiten: Preise, Kontakt, Über uns

### Container Status
```
Container: edufunds (healthy)
Netzwerk: hetzner-stack_web (172.18.0.21)
Port: 3000/tcp
Labels: traefik.enable=true, traefik.http.routers.edufunds.rule=Host(`edufunds.org`)
```

## Offenes Problem

### Traefik Routing
- Container ist intern erreichbar (`docker exec traefik wget http://edufunds:3000/` funktioniert)
- Externe Requests an https://edufunds.org geben **404**
- Traefik erkennt Labels, routet aber nicht
- `edufunds-staging` (nginx) wurde gestoppt (konnte kollidiert haben)

## Nächste Schritte
- Traefik-Konfiguration debuggen
- Oder: Alternative Deployment-Strategie (nginx reverse proxy)
- Oder: Port-Konfiguration prüfen

## Learnings
- Docker Labels sind korrekt gesetzt
- Container health ist ok
- Problem liegt bei Traefik-Router-Konfiguration
